# تشغيل نظام غيث — P6.6 (تسليم بمصدر تشغيل واحد)

## لماذا هذه النسخة؟
النسخ السابقة كان فيها **ازدواج معماري** داخل نفس الـ ZIP:
- تطبيق فعلي: `ghaith-api/`
- + مجلدات جذرية (`kernel/`, `modules/`, `migrations/`...) تبدو كتطبيق ثاني.

هذا يسبب ارتباك، وتعارض أسماء، وتشتت migrations وRBAC.

## ما الذي تغير هنا؟
- ✅ **التشغيل الرسمي الوحيد**: `ghaith-api/`
- 🗂️ **كل ما كان على الجذر سابقًا** تم تجميعه داخل:
  - `legacy_root_pack/`
  كمحتوى مرجعي/أرشيف (NOT RUNTIME)

## التشغيل السريع
1) ادخل مجلد التطبيق:
```bash
cd ghaith_system/ghaith-api
```
2) جهّز ملف البيئة (اختياري ولكن مُستحسن):
```bash
cp .env.example .env
```

3) ثبّت المكتبات:
```bash
npm install
```
4) شغّل فحص الحوكمة السريع:
```bash
npm run system:check
```

5) شغّل السيرفر:
```bash
npm run start
```
6) اختبار الصحة:
```bash
curl http://localhost:3003/api/health
```

## تشغيل عبر Docker (اختياري)
من داخل `ghaith_system/ghaith-api`:
```bash
docker compose up -d --build
```
ثم:
```bash
curl http://localhost:3003/api/health
```

## أين أصبحت الملفات "الزائدة"؟
داخل:
- `ghaith_system/legacy_root_pack/`

> هذه ليست جزء من التشغيل. الهدف منها حفظ أي شغل سابق/ملاحظات دمج بدون ما تربك مصدر التشغيل.

## ملاحظة مهمة
هذه النسخة تركّز على **إغلاق مشكلة التسليم والتقسيم**.
الخطوة التالية هي **تشديد جاهزية الإنتاج** (Prod Readiness) + تنظيف أي بقايا مسارات توافق، راجع:
- `docs/PROD_READINESS.md`
